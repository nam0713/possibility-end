{
  "name": "possibility-end-cinematic-universe",
  "version": "6.0.0",
  "private": true,
  "description": "가능성의 끝 — continuous reading, a shared light and a spatial score",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "node build.mjs",
    "preview": "node server.mjs",
    "check": "node --check src/app.js && node --check src/cosmos.js && node --check build.mjs && node --check server.mjs && node --check src/ending.js && node --check src/audio.js && node --check src/cinematic.js"
  },
  "engines": {
    "node": ">=18"
  }
}
